{
  "name": "taskflow",
  "version": "2.3.0",
  "description": "TaskFlow - Premium task management with focus mode, voice input, and smart scheduling",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
